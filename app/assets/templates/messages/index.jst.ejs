<h1>All Messages</h1>

<ul class="messages">
  <h4>Sent Messages</h4>
  <% messages.each(function(msg){ %>
    <% if(msg.get("sender_id") === currentUser.id){ %>
      <ul class="sent-msg">
        <li>Title: <%= msg.escape("title") %></li>
        <li>Body: <%= msg.escape("body") %></li>
        <li>From: <%= msg.receiver().escape("username") %></li>
        <li>Sent: <%= new Date(msg.escape("created_at")) %></li>
      </ul>
    <% } %>
  <% }) %>

  <h4>Received Messages</h4>
  <% messages.each(function(msg){ %>
    <% if(msg.get("receiver_id") === currentUser.id){ %>
      <ul class="received-msg">
        <li>Title: <%= msg.escape("title") %></li>
        <li>Body: <%= msg.escape("body") %></li>
        <li>From: <%= msg.sender().escape("username") %></li>
        <li>Sent: <%= new Date(msg.escape("created_at")) %></li>
      </ul>
    <% } %>
  <% }) %>
</ul>
